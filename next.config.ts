// next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
  eslint: {
    // ✅ Ignore ESLint errors during builds (good for quick demos)
    ignoreDuringBuilds: true,
  },
};

module.exports = nextConfig;
